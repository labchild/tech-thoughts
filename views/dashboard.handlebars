<section class="content-wrapper">
    <h2 class="lead text-secondary">Dashboard</h2>
    <div class="text-center">
        {{#each posts as |post|}}
        {{>post-card post}}
        {{/each}}
    </div>
</section>